name: CI - tests

on:
  push:
    branches:
      - main
      - 'wf-*'
      - 'wf-mtc.*'
      - 'wf-mtc.4/*'
      - 'wf-mtc.4/tests-runner-snapshots'
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Run test suite
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
        env:
          CI: true
