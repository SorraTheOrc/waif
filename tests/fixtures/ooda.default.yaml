jobs:
  - id: hello-5s
    name: Hello every 5s (test fixture)
    command: "node -e \"console.log('hello')\""
    schedule: "*/5 * * * * *"
    capture:
      - stdout
    timeout_seconds: 5
    retention:
      keep_last: 2
